CREATE TABLE tbl (id INTEGER,name VARCHAR(20),description VARCHAR(100));
INSERT INTO tbl VALUES (0,'new', '#12345');
SELECT * FROM tbl;
system trigger true;
system trigger false;
system timeout;
system alright;